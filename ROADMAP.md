# Z-1080™ Roadmap

Оновлення: 2025-11-26  
Статус: активна розробка

---

## 🔥 Етап 1 — Core CLI

Ціль: робочий консольний інструмент генерації паролів.

### Завдання

- ✔️ Базовий CLI-генератор (довжина, типи символів).
- ✔️ Обробка аргументів командного рядка (довжина, прапорці).
- 🧪 Режим **obмеженого / кастомного алфавіту**:
  - окремий прапорець (`--restricted` або подібний);
  - можливість вказати свій набір символів.
- ❌ Збереження в файл:
  - звичайний `.txt`;
  - у перспективі — зашифрований `.enc`.

---

## 🧪 Етап 2 — GUI

Ціль: мінімалістичний, але зручний графічний інтерфейс.

План:

- Вибір стеку: **Tkinter** (простий старт) або **PySide6 / PyQt**.
- Поля:
  - довжина пароля;
  - прапорці: цифри, великі літери, малі літери, спецсимволи;
  - галочка «обмежений алфавіт» та поле для введення алфавіту.
- Кнопки:
  - **Generate**;
  - **Copy to clipboard**;
  - **Save to file** (на базі логіки з CLI).
- Базові повідомлення про помилки (наприклад, «порожній алфавіт»).

Орієнтовно: **Q1–Q2 2026**.

---

## ♻️ Етап 3 — Security / Lab Integration

Ціль: використання Z-1080™ як модуля у навчальних та PenTest-лабораторіях.

Ідеї:

- Генерація декількох паролів пакетно (для тестових облікових записів).
- Режими, адаптовані під:
  - брутфорс-демонстрації;
  - приклади «сильних» vs «слабких» паролів.
- Інтеграція з Python-скриптами для автоматизації:
  - імпорт як `import z1080_core`;
  - функції на кшталт `generate_password(config)`.

---

## 🧊 Довгострокові цілі

- Готові збірки:
  - `.exe` для Windows (PyInstaller / Nuitka);
  - `.deb` / AppImage для Linux.
- Простий API:
  - офіційний модуль `z1080` через `pip`.
- Інтеграція з іншими інструментами:
  - використання у власних утилітах безпеки;
  - можливі профілі для різних кейсів (адміністратор, dev, користувач).

---

## 🧩 Поперечні задачі (йдуть паралельно)

| Компонент         | Статус |
|-------------------|--------|
| Документація      | 🧪     |
| Приклади / snippets | 🧪   |
| Автотести (pytest)| ❌     |
| CI/CD (GitHub Actions) | ❌ |

---

## 🏁 Поточний пріоритет

1. Доробити **CLI + обмежений алфавіт** у стабільну версію.
2. Додати **надійне збереження в файл**.
3. Після стабілізації — перейти до **мінімального GUI**.
